spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/album_copa2026
    username: postgres
    password: postgres

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    sql:
      init:
        mode: always

  h2:
    console:
      enabled: true
      path: /h2-console

jwt:
  secret: ${JWT_SECRET:pA9FvQhHnC5e0M0vT7Rz8XKqYJ5fQ1P8ZsLk9E3R2W4}
  expiration: 3600000 # 1h em ms
